<Window x:Class="MdEditor.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preferences" Height="230" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Font" VerticalAlignment="Center" Margin="0,0,12,10"/>
        <ComboBox x:Name="FontCombo" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"
                  IsEditable="False" SelectionChanged="FontCombo_SelectionChanged"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Size (px)" VerticalAlignment="Center" Margin="0,0,12,10"/>
        <ComboBox x:Name="SizeCombo" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"
                  IsEditable="True" SelectionChanged="SizeCombo_SelectionChanged"
                  LostFocus="SizeCombo_LostFocus"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Spellcheck" VerticalAlignment="Center" Margin="0,0,12,10"/>
        <ComboBox x:Name="SpellCombo" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10"
                  SelectionChanged="SpellCombo_SelectionChanged"/>

        <Button Grid.Row="4" Grid.Column="1" Content="Close" Width="80" HorizontalAlignment="Right"
                IsCancel="True" IsDefault="True" Click="Close_Click"/>
    </Grid>
</Window>
